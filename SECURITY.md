# Security Policy

Nexora Free Lite is a static front-end template. It has no authentication, storage, payment processing, or form backend.

When adding a backend, validate all input server-side, use CSRF protection where applicable, enforce HTTPS, keep dependencies current, and never put private API keys in front-end files.

Report template security issues privately through the repository owner's GitHub profile without including secrets or personal data in a public issue.
